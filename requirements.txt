requests==2.31.0
beautifulsoup4==4.12.2
pydantic==2.11.3
pydantic-settings==2.9.1
python-dateutil==2.8.2
tenacity==8.2.3
lxml==5.3.0
selenium==4.15.2
webdriver-manager==4.0.1
fastapi==0.115.5
uvicorn[standard]==0.34.0
jinja2==3.1.2
python-dotenv==1.0.0
click==8.1.7
pyyaml==6.0.2

# Data processing
# duckdb 1.1.x: last series compatible with dbt-duckdb 1.7.4 (<1.2.0),
# and has native arm64 wheels for Python 3.13
duckdb==1.1.3
pandas==2.2.3
pyarrow==19.0.0

# dbt — Silver → Gold transformations (Phase 3)
# dbt-duckdb 1.7.x requires duckdb>=0.10.0,<1.2.0 — compatible with 1.1.3
dbt-duckdb==1.7.4
# protobuf>=5 removed MessageToJson(including_default_value_fields=...) used by dbt 1.7.x
protobuf>=3.20.3,<5.0.0

# GCP Production
google-cloud-bigquery==3.20.1
db-dtypes==1.2.0
gcsfs==2024.3.1

# MCP Server
mcp==1.27.0

# AI — Gemini API for intelligent role variant generation (free tier)
google-genai

# Testing
pytest==7.4.3
httpx==0.27.2
